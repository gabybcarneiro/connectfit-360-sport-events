spring:
  application:
    name: sportsevents
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/connectfit?currentSchema=sports}
#    username: ${SPRING_DATASOURCE_USERNAME:connectfit}
#    password: ${SPRING_DATASOURCE_PASSWORD:Connect360}}
    username: ${SPRING_DATASOURCE_USERNAME:admin}
    password: ${SPRING_DATASOURCE_PASSWORD:admingabriela}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
    open-in-view: false
server:
  port: ${SERVER_PORT:8080}
springdoc:
  api-docs:
    path: /api-docs
